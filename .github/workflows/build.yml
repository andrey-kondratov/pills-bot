name: Build

on:
  pull_request:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Restore tools
        run: dotnet tool restore
      - name: Build images
        run: dotnet cake docker.cake
