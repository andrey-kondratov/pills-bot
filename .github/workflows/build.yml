name: Build

on:
  pull_request:
    branches: 
      - master
  push:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=pr
      - name: Restore tools
        run: dotnet tool restore
      - name: Build and push
        run: echo "${{ github.token }}" | dotnet cake --target Push --server ghcr.io --registry ${{ github.repository_owner }} --username ${{ github.repository_owner }} --tag ${{ steps.meta.outputs.version }}
